<%- include ('partials/header.ejs') %>
<%- include ('partials/navbar.ejs') %>
<h2>Minha main role (a mais triste também)</h2>
<p>cara</p>
<table class="table table-dark table-striped table-hover table-bordered">
        <thead class="table-dark">
            <tr>
            <th>Campeão:</th>
             <th>Classe:</th>
            <th>Motivo:</th>
            </tr>
            </thead>
            <tbody>
            <!-- FOREACH -->
            <% junglers.forEach(junglers => { %>
            <tr>
                <td><%= junglers.campeao %></td> 
                <td><%= junglers.classe %> </td>
                <td><%= junglers.motivo %></td> 
            </tr>
        <% }) %>  
            </tbody>
        </table>
<%- include ('partials/footer.ejs') %>