<%- include ('partials/header.ejs') %>
<%- include ('partials/navbar.ejs') %>

<h2>Fiquei sem ideia da terceira</h2>

<table class="table table-dark table-striped table-hover table-bordered">
        <thead class="table-dark">
            <tr>
            <th>Campeão:</th>
             <th>Classe:</th>
            <th>Motivo:</th>
            </tr>
            </thead>
            <tbody>
            <!-- FOREACH -->
            <% midLaners.forEach(midLaners => { %>
            <tr>
                <td><%= midLaners.campeao %></td> 
                <td><%= midLaners.classe %> </td>
                <td><%= midLaners.motivo %></td> 
            </tr>
        <% }) %>  
            </tbody>
        </table>

<%- include ('partials/footer.ejs') %>